#!/bin/bash
# This script is designed for checking all the
# dependencies required by terraform to function properly.
set -e

# Check if terraform is installed
# Check if aws cli is installed
# Check if named profile is existing
# Check if state s3 is existing
# Check if lock dynamodb is existing
